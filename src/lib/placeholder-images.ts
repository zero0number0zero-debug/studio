export type ImagePlaceholder = {
  id: string;
  description: string;
  imageUrl: string;
  imageHint: string;
};

// This file is kept for type definition, but the data is no longer stored here.
export const PlaceHolderImages: ImagePlaceholder[] = [];
